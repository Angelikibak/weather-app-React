import React from 'react';

const Loading = () => (
      <div>
            <h1>
                  The app is Loading
                  <span>.</span>
                  <span>.</span>
                  <span>.</span>
            </h1>
      </div>
);

export default Loading;
